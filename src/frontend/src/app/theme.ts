import type { ThemeConfig } from 'antd';

export const appTheme: ThemeConfig = {
  token: {
    // intentionally empty for now — design system tweaks land here
    // when Claude Design produces the first iteration's tokens
  },
  components: {
    // component-level overrides go here when needed
  },
};
